{
  "name": "discord-clone-server",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "vitest run",
    "test:watch": "vitest",
    "lint": "eslint src/"
  },
  "dependencies": {
    "discord-clone-shared": "*",
    "fastify": "^5.7.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "pino-pretty": "^13.1.3",
    "tsx": "^4.21.0",
    "vitest": "^4.0.0"
  }
}
